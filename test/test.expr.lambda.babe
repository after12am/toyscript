/+
(function (x) {
    return x * x
})

(function (x) {
    x = x || 1
    return x * x
})

(function (x) {
    x = x || 'message'
    return x
})

(function (x) {
    x = x || [
        1,
        2,
        3
    ]
    return x
})

(function (x, y) {
    return x * y
})

var a = function (x) {
    return x * x
}
+/
(x): x * x

(x = 1): x * x

(x = "message"): x

(x = [1, 2, 3]): x

(x, y): x * y

a = (x): x * x